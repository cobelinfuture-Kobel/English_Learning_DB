# RAZ-AW-S11 Reading Authority Intake Query Index Builder Implementation

## 1. Task

`RAZ-AW-S11_ReadingAuthorityIntake_QueryIndexBuilderImplementation`

This implementation creates the first deterministic, offline Reading Authority intake query index for RAZ-derived reading candidates.

The task does **not** promote content into final Reading Authority. It creates a static intake layer that later Reading Authority, Content Query Layer, ULGA, Antigravity, and Learning Opportunity Binding stages can query.

## 2. Preflight Result

| Check | Result |
|---|---|
| Repository write target | `cobelinfuture-Kobel/English_Learning_DB` |
| Branch | `raz-aw-s11-reading-intake-index` |
| Default branch | `main` |
| Runtime / learner state touched | No |
| Dashboard / app files touched | No |
| Network calls in builder | No |
| LLM generation in builder | No |
| Authority promotion | No |

## 3. Files Inspected

- `README.md`
- `ulga/builders/build_corpus_source_inventory.py`
- `ulga/validators/validate_corpus_source_inventory.py`
- `tests/ulga/test_corpus_source_inventory.py`
- `ulga/validators/validate_raz_reading_authority_intake_query_index.py`
- `tests/ulga/test_raz_reading_authority_intake_query_index.py`

These files were used as local style references for builder, validator, report, and test layout.

Important conflict found: `ulga/validators/validate_raz_reading_authority_intake_query_index.py` and `tests/ulga/test_raz_reading_authority_intake_query_index.py` already exist for `RAZ-AW-S10A_ReadingAuthorityIntake_QueryIndexContractImplementation`. They were intentionally not overwritten.

## 4. Files Created

- `ulga/builders/build_raz_reading_authority_intake_query_index.py`
- `ulga/validators/validate_raz_reading_authority_intake_query_index_builder_output.py`
- `tests/ulga/test_raz_reading_authority_intake_query_index_builder_output.py`
- `docs/ulga/RAZ_AW_S11_READING_AUTHORITY_INTAKE_QUERY_INDEX_BUILDER_IMPLEMENTATION.md`

Generated by local execution of the builder:

- `ulga/graph/raz_reading_authority_intake_query_index.json`
- `ulga/reports/raz_reading_authority_intake_query_index_summary.json`

## 5. Source Artifacts Consumed

The builder discovers existing offline JSON / JSONL files under these roots when present:

```text
work/raz/
raz_output_jsons/
output/raz/
ulga/graph/
ulga/reports/
```

It only considers files that appear to be RAZ / level-derived artifacts and excludes its own output files.

Supported intake source types:

```text
sentence_candidate
page_unit
reuse_unit_candidate
normalized_reading_unit
enriched_reading_unit
```

If no matching source artifacts are available in a local checkout, the builder still writes a stable empty index with `PASS_WITH_WARNINGS` and `no_raz_source_json_or_jsonl_files_discovered`.

## 6. Output Schema

Main output:

```text
ulga/graph/raz_reading_authority_intake_query_index.json
```

Top-level contract:

```json
{
  "schema_version": "RAZ_READING_AUTHORITY_INTAKE_QUERY_INDEX_V1",
  "builder_task": "RAZ-AW-S11_ReadingAuthorityIntake_QueryIndexBuilderImplementation",
  "generated_at": "<iso8601>",
  "source_policy": {
    "offline_static_only": true,
    "generated_content": false,
    "authority_promotion": false,
    "candidate_only_preserved": true
  },
  "levels": [],
  "items": [],
  "query_indexes": {},
  "summary": {}
}
```

Each item preserves:

- deterministic `intake_id`
- source type
- RAZ level
- book/page metadata when available
- clean text
- sentence count
- sentence candidate links when available
- source traceability
- query tags
- candidate-only authority boundary

## 7. Query Index Contract

The builder always emits these query index keys, even when some are empty:

```text
by_level
by_book_id
by_level_and_book
by_source_type
by_reusability_tag
by_authority_status
by_promotion_status
by_sentence_count_bucket
by_multi_sentence_status
by_theme_hint
by_grammar_tag
by_pattern_tag
by_vocabulary_tag
```

Sentence count buckets:

```text
single_sentence
two_sentences
three_to_five_sentences
six_plus_sentences
unknown
```

## 8. Candidate-Only / No-Promotion Boundary

All intake items are emitted as:

```json
{
  "authority_status": "candidate_only",
  "promotion_status": "not_promoted",
  "generated_content": false
}
```

This is intentional.

This stage is an intake/query-index stage, not a final Reading Authority promotion stage. Reuse units, short reading seeds, writing model seeds, dialogue rewrite seeds, exercise seeds, and assessment seeds remain queryable candidates only.

## 9. Validation Contract

The S11 builder-output validator is:

```text
ulga/validators/validate_raz_reading_authority_intake_query_index_builder_output.py
```

It checks:

1. Required files exist.
2. JSON parses.
3. Top-level schema version matches.
4. Required item fields exist.
5. Intake IDs are stable and unique.
6. Query index references point only to known item IDs.
7. No item is promoted.
8. `generated_content` remains false.
9. Source traceability exists.
10. Query tag shapes are valid.
11. Sentence count bucket logic is consistent.
12. Embedded summary matches summary file.
13. Summary counts match indexed items.
14. Empty index categories are valid.
15. S10A-derived items from `ulga/graph/raz_reading_authority_intake_candidates.json` do not remain at `UNKNOWN`.
16. S10A-derived items preserve `source_traceability.source_record_id`.
17. Reusability tags do not contain dict-stringified metadata blobs.

## 10. Test Coverage

Test file:

```text
tests/ulga/test_raz_reading_authority_intake_query_index_builder_output.py
```

Coverage includes:

- builder can run offline
- validator can pass
- output JSON exists and parses
- top-level keys exist
- source policy preserves offline/candidate-only/no-promotion boundary
- required query indexes exist even when empty
- index references point only to valid intake IDs
- summary counts match item counts
- sentence count bucket logic is deterministic
- multi-sentence buckets match items
- reusability tags are indexed when present
- S10A source level / normalized level preservation
- S10A `reading_intake_id` propagation into `source_traceability.source_record_id`
- nested `tags.reusability_tags` extraction
- dict-shaped `tags` metadata is not stringified into `by_reusability_tag`

## 11. Local Execution Commands

Run from repository root:

```bash
python ulga/builders/build_raz_reading_authority_intake_query_index.py
python ulga/validators/validate_raz_reading_authority_intake_query_index_builder_output.py
pytest tests/ulga/test_raz_reading_authority_intake_query_index_builder_output.py
```

Optional existing S10A contract validation remains separate:

```bash
python ulga/validators/validate_raz_reading_authority_intake_query_index.py
pytest tests/ulga/test_raz_reading_authority_intake_query_index.py
```

## 12. Known Limitations

- The builder uses permissive static discovery because RAZ derived artifacts may exist under multiple historical paths.
- It does not generate content.
- It does not infer missing grammar / pattern / vocabulary tags when source artifacts do not already provide them.
- It does not promote candidate readings into final Reading Authority.
- It does not bind readings to Learning Opportunity records; that belongs to a later stage.
- It does not create Reading Dependency Graph edges.

## 13. Recommended Next Task

```text
RAZ-AW-S12_ReadingAuthorityIntake_QueryIndexReadbackQA
```

The next task should run the builder and validator in the real local checkout, inspect the generated index counts, verify source coverage by RAZ level, and decide whether additional source path adapters are needed before any Reading Authority promotion task.

## 14. S11 Patch Follow-up

S12 readback QA against the real local artifact exposed three concrete S11 builder defects and one source-discovery warning issue:

1. S10A structured intake records were not recognized at the top level, so the builder descended into nested `text` blocks and lost parent metadata.
2. `extract_level()` did not preserve `normalized_level`, which contributed to `UNKNOWN` coverage for S10A-derived items.
3. `extract_source_id()` did not prioritize `reading_intake_id` / `artifact_pointer.source_record_id` / `source_record_id`.
4. `extract_reusability_tags()` could stringify dict-shaped metadata through `record["tags"]`.

Patch result:

- S10A top-level structured records are now recognized directly.
- S10A-derived items preserve `source_level` / `normalized_level`.
- S10A-derived items carry `reading_intake_id` into `source_traceability.source_record_id`.
- Nested `tags.reusability_tags` is extracted, but dict-shaped `tags` metadata is no longer stringified into query tags.
- Discovery skips bridge / review / linkage / report-style artifacts that were producing misleading `no_candidate_records_found` warnings.

Evidence expected after rebuild:

- `UNKNOWN` count for `ulga/graph/raz_reading_authority_intake_candidates.json` derived items: `0`
- malformed dict-stringified reusability tags: `0`
- missing `source_traceability.source_record_id` for S10A-derived items: `0`
- candidate-only / no-promotion / no-generated-content boundaries unchanged
